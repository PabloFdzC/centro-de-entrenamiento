<% for(let servicio in lista){ %>
  <div class="card mx-auto background cardLista mt-2">
    <div class="card-body" id="cardB">
      <div class="row justify-content-between">
        <div class="col-6">
          <h2 class="card-title tituloElementoLista2"><%= servicio.getNombre() %></h2>
        </div>
        <div class="col-4">
          <div class="row">
            <div class="d-grid col-6">
              <button type="submit" class="btn btn-secondary">Eliminar</button>
            </div>
            <div class="d-grid col-6">
              <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#modalCrearServicio">Editar</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% } %>